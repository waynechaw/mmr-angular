import { ChampionFilterPipe } from './champion-filter.pipe';

describe('ChampionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChampionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
